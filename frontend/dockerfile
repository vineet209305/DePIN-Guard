# frontend/Dockerfile
FROM node:18-slim

WORKDIR /app

# Install dependencies first for caching
COPY package*.json .
RUN npm install

# Copy all source files
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# --host flag exposes Vite outside the container
CMD ["npm", "run", "dev", "--", "--host"]